import { ResultSummaryPage } from "./ResultSummaryPage"

export default function ResultSummary(/**{ params }: { params: { match_id: string } }*/) {
  return <ResultSummaryPage /**params={params} */ />
}
